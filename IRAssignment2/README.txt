Run the file movies.py

Then run the file utilityfunctions.py

Then run csvreader.py file

Then run the main.py file

Then the matrix is saved as pickle objects.

In order to run collaborative now:
specific_user.py then mention userids

In order to run SVD run :
main_svd.py ,no need for inputs in this.

In order to run CUR run:
cur_decomposition.py
